---
phase: 04-dashboard-and-deployment
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - app/services/escalation_service.py
  - app/dashboard/routes.py
  - app/templates/dashboard/index.html
  - app/templates/dashboard/partials/escalation_panel.html
  - app/static/css/main.css
  - .github/workflows/deploy.yml
  - app/templates/base.html
autonomous: false

must_haves:
  truths:
    - "User with a claim > $25K sees a lawyer escalation panel on the dashboard"
    - "User with a corporate defendant sees the escalation panel"
    - "User with a tolled or expired limitation period sees the escalation panel"
    - "User without escalation triggers does NOT see the escalation panel"
    - "GitHub Actions workflow deploys to Cloud Run on push to main using Workload Identity Federation"
    - "WCAG 2.0 Level AA fundamentals pass manual keyboard and contrast check"
    - "Regulatory sign-off checklist exists as a trackable artifact"
  artifacts:
    - path: "app/services/escalation_service.py"
      provides: "is_escalation_required(claim) -> bool pure function"
      exports: ["is_escalation_required"]
    - path: "app/templates/dashboard/partials/escalation_panel.html"
      provides: "Lawyer escalation CTA panel"
    - path: ".github/workflows/deploy.yml"
      provides: "GitHub Actions CI/CD with WIF auth, Docker build, Cloud Run deploy"
  key_links:
    - from: "app/dashboard/routes.py"
      to: "app/services/escalation_service.py"
      via: "is_escalation_required(claim) called per claim in index route"
      pattern: "is_escalation_required"
    - from: "app/templates/dashboard/index.html"
      to: "app/templates/dashboard/partials/escalation_panel.html"
      via: "Jinja2 include conditional on escalation flag"
      pattern: "escalation_panel"
    - from: ".github/workflows/deploy.yml"
      to: "Dockerfile"
      via: "docker build step"
      pattern: "docker build"
---

<objective>
Add the lawyer escalation pathway to the dashboard and harden the deployment pipeline for production.

Purpose: DASH-04 -- complex cases (claim > $25K, corporate defendant, flagged limitation period) need a clear handoff to the partner lawyer. The deployment pipeline (GitHub Actions + WIF) must be production-ready, and WCAG/AODA fundamentals must be verified.

Output: Escalation service + panel, updated GitHub Actions workflow with secrets, WCAG audit pass, regulatory sign-off checklist.
</objective>

<execution_context>
@C:\Users\Bry\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Bry\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dashboard-and-deployment/04-RESEARCH.md
@.planning/phases/04-dashboard-and-deployment/04-01-SUMMARY.md

@app/__init__.py
@app/config.py
@app/models/claim.py
@app/ontario_constants.py
@app/services/limitation_service.py
@app/dashboard/routes.py
@app/templates/dashboard/index.html
@app/templates/base.html
@app/static/css/main.css
@.github/workflows/deploy.yml
@cloudbuild.yaml
@Dockerfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escalation service, escalation panel, and deployment hardening</name>
  <files>
    app/services/escalation_service.py
    app/dashboard/routes.py
    app/templates/dashboard/partials/escalation_panel.html
    app/templates/dashboard/index.html
    app/static/css/main.css
    .github/workflows/deploy.yml
    app/templates/base.html
  </files>
  <action>
    **Escalation Service:**

    1. Create `app/services/escalation_service.py`:
       - `from __future__ import annotations` at top.
       - `is_escalation_required(claim) -> bool` pure function.
       - Returns True if ANY of these conditions are met:
         a. Claim amount > INFREQUENT_CLAIMANT_LIMIT ($25,000): Read from `claim.step_data.get("amount", {}).get("amount", 0)`. Parse to int, default 0 on error.
         b. Corporate defendant: `claim.step_data.get("opposing_party", {}).get("party_type") == "business"`.
         c. Limitation requires lawyer review or is expired: `claim.step_data.get("limitation", {}).get("status")` in `("requires_lawyer_review", "expired", "ultimate_expired")`.
       - `get_escalation_reasons(claim) -> list[str]` — returns human-readable strings explaining WHY escalation is recommended (e.g., "Your claim exceeds $25,000", "Your claim involves a corporate defendant", "Your limitation period requires legal review"). This is for display in the panel.
       - Import `INFREQUENT_CLAIMANT_LIMIT` from `app.ontario_constants`.

    2. Update `app/dashboard/routes.py`:
       - Import `is_escalation_required, get_escalation_reasons` from `app.services.escalation_service`.
       - In the `index()` route, compute escalation data: `claim_escalations = {c.id: {"required": is_escalation_required(c), "reasons": get_escalation_reasons(c)} for c in claims}`. Pass `claim_escalations` to the template.

    3. Create `app/templates/dashboard/partials/escalation_panel.html`:
       - Receives `escalation` dict (with "required" bool and "reasons" list) and `claim`.
       - Only renders content if `escalation.required` is true.
       - Panel structure: A bordered card with a distinct styling (navy background with white text, or white card with navy left border). Heading: "This Case May Need a Lawyer". Body text: "Based on your case details, we recommend consulting with a licensed lawyer before proceeding." Then a bulleted list of `escalation.reasons`. CTA button: "Request a Consultation" (links to a mailto: with a subject line including the claim type, or a placeholder URL — for POC, use `mailto:info@bryanandbryan.ca?subject=Consultation Request — Case #{claim.id}`). Below the CTA, a small text: "Our partner lawyer can review your case and provide legal advice tailored to your situation."
       - Disclaimer micro-text at bottom of panel: "This recommendation is based on general case characteristics and does not constitute legal advice."

    4. Update `app/templates/dashboard/index.html`:
       - For each claim, after the timeline section, check `claim_escalations[claim.id].required`. If true, include the escalation panel partial: `{% include "dashboard/partials/escalation_panel.html" with context %}` passing `escalation=claim_escalations[claim.id]` and `claim=claim`.

    5. Add escalation CSS to `app/static/css/main.css`:
       - `.escalation-panel`: background var(--color-bg), border 1px solid var(--color-primary), border-left 4px solid var(--color-primary), border-radius var(--radius), padding 1.25rem, margin-top 1.5rem.
       - `.escalation-panel h3`: color var(--color-primary), font-size 1.125rem, margin 0 0 0.75rem.
       - `.escalation-panel p`: color var(--color-text), margin 0 0 0.75rem, line-height 1.5.
       - `.escalation-reasons`: list-style disc, padding-left 1.25rem, margin 0.5rem 0 1rem. `li` margin-bottom 0.25rem, font-size 0.9375rem.
       - `.escalation-cta`: display inline-flex, align-items center, justify-content center, padding 0.75rem 1.5rem, background-color var(--color-primary), color #ffffff, text-decoration none, border-radius var(--radius), font-weight 600, min-height var(--touch-target).
       - `.escalation-cta:hover`: background-color var(--color-primary-light).
       - `.escalation-disclaimer`: font-size 0.8125rem, color var(--color-text-secondary), margin-top 0.75rem, font-style italic.

    **Deployment Hardening:**

    6. Update `.github/workflows/deploy.yml` to be production-ready:
       - Keep existing structure but add:
         a. A `test` job that runs BEFORE deploy: Install requirements + pytest, run `python -m pytest tests/ -v --tb=short` (tests dir may not exist yet — add `|| true` to not block deploy for now, but log the result).
         b. In the `deploy` job, add `needs: [test]` so deploy only runs after tests pass.
         c. In the `deploy-cloudrun` step, add `secrets` configuration matching the existing `cloudbuild.yaml` pattern: `DATABASE_URL=claimpilot-db-url:latest`, `ANTHROPIC_API_KEY=anthropic-api-key:latest`, `SECRET_KEY=flask-secret-key:latest`.
         d. Add environment variables for the Cloud Run service: `FLASK_CONFIG=production`.
         e. Add `--min-instances 0 --max-instances 3 --memory 512Mi --cpu 1 --port 8080` flags to match existing cloudbuild.yaml configuration, using the `flags` input of deploy-cloudrun@v3.
       - Add a comment at the top: `# Prerequisites: WIF pool/provider must be created in GCP. See .planning/phases/04-dashboard-and-deployment/04-RESEARCH.md for setup commands.`
       - Add a comment noting that the Cloud Build trigger in GCP Console should be disabled to avoid double-deploys.

    7. Verify `app/templates/base.html` has proper WCAG structure:
       - Confirm skip-nav link exists (it does).
       - Confirm all nav links have proper focus styles (they do via :focus-visible).
       - Ensure the nav links added in 04-01 have `aria-current="page"` on the active link. Add a Jinja2 macro or conditional: `{% if request.endpoint == 'dashboard.index' %}aria-current="page"{% endif %}` for each nav link.
  </action>
  <verify>
    Run `python -c "from app.services.escalation_service import is_escalation_required, get_escalation_reasons; print('OK')"` — should print OK.
    Run `python -c "
from app import create_app
app = create_app()
with app.app_context():
    from app.models.claim import Claim
    from app.services.escalation_service import is_escalation_required
    # Test with a mock-like claim
    class FakeClaim:
        step_data = {'amount': {'amount': '30000'}, 'opposing_party': {'party_type': 'individual'}, 'limitation': {'status': 'within_period'}}
    assert is_escalation_required(FakeClaim()) == True  # amount > 25K
    class FakeClaim2:
        step_data = {'amount': {'amount': '5000'}, 'opposing_party': {'party_type': 'individual'}, 'limitation': {'status': 'within_period'}}
    assert is_escalation_required(FakeClaim2()) == False
    print('Escalation logic OK')
"` — should print "Escalation logic OK".
    Verify `.github/workflows/deploy.yml` has valid YAML structure: `python -c "import yaml; yaml.safe_load(open('.github/workflows/deploy.yml')); print('YAML OK')"` (requires PyYAML — if not available, just visually inspect).
  </verify>
  <done>
    Escalation service detects complex cases (>$25K, corporate defendant, expired/reviewed limitation). Escalation panel renders on dashboard for qualifying claims with reasons and CTA. GitHub Actions workflow includes test job, secrets, and Cloud Run config matching existing cloudbuild.yaml. Nav links have aria-current for active page.
  </done>
</task>

<task type="auto">
  <name>Task 2: WCAG audit checklist and regulatory sign-off document</name>
  <files>
    app/static/css/main.css
    app/templates/base.html
  </files>
  <action>
    1. **WCAG 2.0 Level AA audit** — walk through the existing pages and the new dashboard, verifying these criteria. Fix any issues found:

       a. **1.1.1 Non-text content**: Verify all images/icons have alt text. The current UI uses no images — confirm no icons were added without alt. If any SVG icons are in nav, add aria-hidden="true" and visually hidden label text.

       b. **1.3.1 Info and Relationships**: Verify timeline uses `<ol>` (not divs). Verify all form inputs have associated `<label>` elements (use `for` attribute matching `id`). Verify tables have `<th>` headers. Verify heading hierarchy (h1 > h2 > h3, no skipped levels on dashboard).

       c. **1.4.3 Contrast (Minimum)**: Verify 4.5:1 contrast ratio for all text. The existing palette (navy #1B2A4A on white = 12.6:1, accent #C9A84C on navy = 5.1:1) passes. Check the new status badges and timeline labels — ensure `.claim-status--draft` text on gray background meets 4.5:1. If text-secondary (#4a5568) on bg-secondary (#f8f9fa) fails, darken text-secondary.

       d. **2.1.1 Keyboard**: Tab through dashboard — all links, buttons, date inputs, and form submit must be reachable. The escalation CTA (anchor tag) must be focusable. Verify no keyboard traps exist (HTMX partial replacement must not break focus).

       e. **2.4.3 Focus Order**: Tab order on dashboard should follow visual order: nav links, overdue banner (if present), claims section, documents section, deadlines/timeline section, escalation panel, footer.

       f. **2.4.6 Headings and Labels**: All form fields (date inputs) must have visible `<label>` elements. The `<label>` must use `for` attribute pointing to the input's `id`.

       g. **3.3.1 Error Identification**: If a user enters an invalid date, the server should not crash — the route already silently skips invalid dates (try/except in save_dates). Add a flash message or inline error if date is invalid format: `flash("Invalid date format. Please use YYYY-MM-DD.", "error")`.

    2. **Fix any CSS issues found** in the audit. Common issues:
       - Ensure `.deadline-item--overdue .deadline-label` text has sufficient contrast against the white background (should be fine with #4a5568).
       - Ensure date input focus states are visible.
       - Ensure the escalation panel CTA button has visible focus ring (already handled by :focus-visible global rule).

    3. **Create regulatory sign-off checklist** at `.planning/phases/04-dashboard-and-deployment/REGULATORY_SIGNOFF.md`:
       - This is a trackable artifact for the lawyer partner to review before launch.
       - Checklist items:
         - [ ] Disclaimer appears on every page (hardcoded in base.html footer, cannot be overridden)
         - [ ] AI assessment uses statistical framing only ("cases with similar characteristics...")
         - [ ] AI assessment feature flag (AI_ASSESSMENT_ENABLED) can be disabled without code deploy
         - [ ] No AI-generated content in document templates (Demand Letter, Form 7A, Form 9A)
         - [ ] PII excluded from AI prompts (build_claim_summary strips names/addresses/emails)
         - [ ] Limitation period logic reviewed for accuracy (discovery doctrine, tolling, municipal notice)
         - [ ] Escalation triggers reviewed (>$25K, corporate defendant, limitation flags)
         - [ ] Redirected claim types reviewed (defamation, landlord-tenant explanations)
         - [ ] WCAG 2.0 Level AA fundamentals verified (keyboard, contrast, labels, headings)
         - [ ] Data retention policy decided (currently no deletion workflow)
         - [ ] Terms of use / privacy policy drafted
       - Header note: "This checklist must be completed by the supervising lawyer before the platform is made available to external users."

    4. **Add /guide link visibility**: Verify the Guide link is in the main nav (added in 04-01 Task 2). Also add a "Learn about the process" link in the dashboard's deadlines section pointing to `/guide` — this helps users understand what each deadline means.
  </action>
  <verify>
    Keyboard test: Start the dev server, load the dashboard, and tab through every interactive element. All must receive visible focus.
    Contrast spot-check: Inspect `.claim-status--draft` computed color values — text color should have >= 4.5:1 ratio against background.
    Run `python -c "import os; assert os.path.exists('.planning/phases/04-dashboard-and-deployment/REGULATORY_SIGNOFF.md'); print('Checklist exists')"`.
    Visually confirm: No heading levels are skipped (h1 for page title, h2 for sections, h3 for sub-sections).
  </verify>
  <done>
    WCAG 2.0 Level AA fundamentals pass: semantic HTML (ol timeline, labeled inputs, heading hierarchy), keyboard navigability, sufficient contrast, error identification on date inputs. Regulatory sign-off checklist created for lawyer review. Guide link accessible from dashboard.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete user dashboard with:
    - Claims list with status badges and action links
    - Documents list with download links
    - Deadline tracker with date entry form (HTMX)
    - CSS timeline visualization (chronological, severity-colored, overdue highlighting)
    - Lawyer escalation panel for complex cases
    - Updated navigation bar (Dashboard, New Assessment, Documents, Guide, Log out)
    - GitHub Actions deployment workflow with WIF and secrets
    - WCAG audit pass
    - Regulatory sign-off checklist
  </what-built>
  <how-to-verify>
    1. Start the dev server: `flask run`
    2. Log in and verify you land on the dashboard (/ redirects)
    3. If no claims exist, verify CTA to start assessment appears
    4. Complete a case assessment through the wizard
    5. Return to dashboard — verify claim appears with correct status
    6. Generate a document (Demand Letter) — verify it appears in documents list with download link
    7. Enter a service date in the deadline form — verify timeline updates showing defence deadline 20 days out
    8. Enter a settlement conference date — verify trial request deadline appears (30 days later)
    9. For escalation: Create a claim with amount > $25,000 — verify escalation panel appears with "exceeds $25,000" reason
    10. Tab through the entire page — all interactive elements should receive visible focus ring
    11. Check nav bar has all links: Dashboard, New Assessment, Documents, Guide, Log out
    12. Click the Guide link — verify /guide page loads
    13. Review `.planning/phases/04-dashboard-and-deployment/REGULATORY_SIGNOFF.md` — confirm checklist is comprehensive
  </how-to-verify>
  <resume-signal>Type "approved" to complete Phase 4, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. Escalation service correctly identifies claims needing lawyer review based on three criteria
2. Escalation panel only renders for qualifying claims, not all claims
3. Escalation reasons list explains WHY in plain language
4. GitHub Actions workflow has valid YAML, includes test job, secrets config, and Cloud Run flags
5. WCAG audit items verified: keyboard navigation, contrast ratios, semantic HTML, form labels
6. Regulatory sign-off checklist is comprehensive and tracks all lawyer-review items
7. No new Python dependencies added
</verification>

<success_criteria>
- Complex cases (> $25K OR corporate defendant OR flagged limitation) show escalation panel
- Simple cases do not show escalation panel
- Escalation panel has clear CTA and plain-language reasons
- GitHub Actions deploys with WIF auth, matching existing cloudbuild.yaml resource config
- All dashboard elements keyboard-accessible
- Heading hierarchy correct (h1 > h2 > h3)
- All form inputs have labels
- Regulatory sign-off checklist exists at known path
</success_criteria>

<output>
After completion, create `.planning/phases/04-dashboard-and-deployment/04-02-SUMMARY.md`
</output>
