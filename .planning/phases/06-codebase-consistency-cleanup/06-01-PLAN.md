---
phase: 06-codebase-consistency-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/__init__.py
  - .github/workflows/deploy.yml
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true

must_haves:
  truths:
    - "Document and DocumentVersion are importable from app.models"
    - "REQUIREMENTS.md traceability table shows Complete for all Phase 2, 3, and 4 requirements"
    - "ROADMAP Phase 1 success criterion 1 says magic-link auth, not email/password"
    - "pytest step in deploy.yml fails the pipeline on test failure"
  artifacts:
    - path: "app/models/__init__.py"
      provides: "Consistent model exports"
      contains: "from app.models.document import Document, DocumentVersion"
    - path: ".github/workflows/deploy.yml"
      provides: "CI test gate enforcement"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Accurate traceability table"
  key_links:
    - from: "app/models/__init__.py"
      to: "app/models/document.py"
      via: "import statement"
      pattern: "from app\\.models\\.document import Document, DocumentVersion"
    - from: ".github/workflows/deploy.yml"
      to: "pytest exit code"
      via: "no || true bypass"
      pattern: "python -m pytest tests/ -v --tb=short$"
---

<objective>
Resolve four tech debt items from v1.0-MILESTONE-AUDIT.md: model export inconsistency, CI safety gap, and documentation staleness in REQUIREMENTS.md and ROADMAP.md.

Purpose: Close all known tech debt before marking v1.0 milestone complete.
Output: Four files edited with surgical precision -- no new features, no new capabilities.
</objective>

<execution_context>
@C:\Users\Bry\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Bry\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-codebase-consistency-cleanup/06-CONTEXT.md
@.planning/phases/06-codebase-consistency-cleanup/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix model exports and CI test gate</name>
  <files>app/models/__init__.py, .github/workflows/deploy.yml</files>
  <action>
    1. Edit `app/models/__init__.py`: Add `from app.models.document import Document, DocumentVersion` after the existing Claim import. Do NOT add DocumentType -- only Document and DocumentVersion per locked decisions. Final file should export: User, Claim, Document, DocumentVersion.

    2. Edit `.github/workflows/deploy.yml` line 36: Change `python -m pytest tests/ -v --tb=short || true` to `python -m pytest tests/ -v --tb=short` (remove ` || true`). This makes test failures block deployment.

    3. Run `python -c "from app.models import User, Claim, Document, DocumentVersion; print('All models importable')"` to verify model exports work.

    4. Run `python -m pytest tests/ -v --tb=short` locally to confirm tests pass -- if tests fail, the CI change would block deploys, so verify green first.
  </action>
  <verify>
    - `python -c "from app.models import User, Claim, Document, DocumentVersion; print('OK')"` prints OK
    - `python -m pytest tests/ -v --tb=short` passes (exit code 0)
    - `grep "|| true" .github/workflows/deploy.yml` returns no matches
    - `grep "DocumentVersion" app/models/__init__.py` returns a match
  </verify>
  <done>Document and DocumentVersion importable from app.models; deploy.yml pytest step has no || true bypass; local tests pass green.</done>
</task>

<task type="auto">
  <name>Task 2: Fix REQUIREMENTS.md traceability and verify ROADMAP wording</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
    1. Edit `.planning/REQUIREMENTS.md` traceability table: Change status from `Pending` to `Complete` for exactly these 15 rows:
       - ASMT-01 through ASMT-07 (Phase 2) -- 7 rows
       - DOCS-01 through DOCS-04 (Phase 3) -- 4 rows
       - GUID-01 through GUID-04 (Phase 3) -- 4 rows

       Do NOT touch Phase 4 DASH rows -- they are already Complete.
       Update the `*Last updated*` line at the bottom to reflect today's date.

    2. Verify `.planning/ROADMAP.md` Phase 1 success criterion 1 (line 28). Research confirmed it already reads "authenticate via magic link" -- verify this is true. If it already says "magic link", no edit needed (log as no-op). If it says "email/password", change to "authenticate via magic link".
  </action>
  <verify>
    - `grep "Pending" .planning/REQUIREMENTS.md` returns no matches (all v1 requirements should be Complete)
    - `grep "magic link" .planning/ROADMAP.md` returns a match on the Phase 1 success criterion line
    - Count of "Complete" in traceability table equals 29 (all v1 requirements)
  </verify>
  <done>All 29 v1 requirements show Complete in traceability table; ROADMAP Phase 1 criterion 1 accurately describes magic-link auth.</done>
</task>

</tasks>

<verification>
All four tech debt items from v1.0-MILESTONE-AUDIT.md resolved:
1. `python -c "from app.models import Document, DocumentVersion"` succeeds
2. `grep -c "Complete" .planning/REQUIREMENTS.md` returns 29 in traceability section
3. `grep "magic link" .planning/ROADMAP.md` shows correct wording
4. `grep "|| true" .github/workflows/deploy.yml` returns empty (no bypass)
</verification>

<success_criteria>
- Zero "Pending" entries in REQUIREMENTS.md v1 traceability table
- All four model classes (User, Claim, Document, DocumentVersion) importable from app.models
- deploy.yml pytest step enforces test gate (no || true)
- ROADMAP Phase 1 success criterion 1 matches actual implementation
- All local tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-codebase-consistency-cleanup/06-01-SUMMARY.md`
</output>
